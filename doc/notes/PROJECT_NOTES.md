# PROJECT NOTES — lan-clip 极简完成记录

每轮 `.claude/loop.md` 闭环结束时追加一行，格式：`#YYYY-MM-DD 完成XX`。

#2026-05-19 完成 Phase 0 工程清理基线：清理 `lein new` 模板失败断言、更新 `project.clj` 元信息、补全 `.gitignore`、改写 `CHANGELOG.md` 与 `doc/intro.md`、脱敏 `resources/config.edn`、README 增加运行限制与安全提示。
#2026-05-19 完成 Phase 1 P0 第一步：新增 `lan-clip.config` 命名空间（默认配置 / 读取 / 合并 / 校验）及 6 个 `lein test` 用例（默认值 / 默认 host / 缺失文件 / 自定义覆盖 / 非法端口 / 默认配置自洽）。
#2026-05-19 完成 Phase 1 P0 第二步：新增 `lan-clip.fingerprint` 命名空间（迁移 `ClipboardData` 记录、`fingerprint`、`changed?`）及 6 个 `lein test` 用例（文本 / 图片字节 / 文件列表 / flavor 变化 / 内容变化 / 相同内容）。
#2026-05-19 完成 Phase 1 P0 第三步：新增 `lan-clip.clipboard` 命名空间（`IClipboard` 协议、`SystemClipboard`、`FakeClipboard`）及 6 个 `lein test` 用例（空状态 / 文本读写 / 图片读写 / 文件列表读写 / available-flavors / 协议实现检查）。
#2026-05-19 完成 Phase 1 P0 第四步：新增 `lan-clip.watcher` 命名空间（可停止的 `start-watcher` / `stop-watcher`，基于 `volatile!` + `future-cancel`）及 4 个 `lein test` 用例（回调执行 / 周期性 / 可停止 / 幂等停止）。
#2026-05-19 完成 Phase 1 P0 第五步：新增 `lan-clip.app` 命名空间（`start!` / `stop!` / `status`，整合配置加载与 watcher 生命周期）及 3 个 `lein test` 用例（启动停止 / 默认配置 / 状态反映）。
#2026-05-19 完成 Phase 2 P0 第一步：新增 `lan-clip.protocol` 命名空间（二进制协议 / HMAC-SHA256 / magic+version 校验）及 6 个 `lein test` 用例（文本往返 / HMAC 成功 / HMAC 失败 / magic 错误 / version 错误 / 截断消息）。metadata 第一版使用 EDN，避免新增依赖。
#2026-05-19 完成 Phase 2 P0 第二步：新增 `lan-clip.socket.protocol-codec` 命名空间（Netty 编解码器 / 4-byte length prefix / `->protocol-encoder` / `->protocol-decoder`）及 6 个 `lein test` 用例（frame 长度前缀 / encoder 有效性 / decoder 完整读取 / decoder 半包累积 / HMAC 拒绝 / 往返）。尚未替换 `server.clj` / `client.clj` 中的 ObjectEncoder/ObjectDecoder。
#2026-05-19 完成 Phase 2 P0 第三步：将 `protocol-codec` 整合进 `server.clj` / `client.clj` / `core.clj`，替换 `ObjectEncoder` / `ObjectDecoder`；`config.clj` 增加 `:secret-key` 默认；`handle-msg` 按 `:content-type` 分发；新增 integration-test 验证 encoder → decoder 往返。图片/文件链路待迁移。
#2026-05-19 完成 Phase 2 P1 第一步：图片链路迁移至新协议。`protocol.clj` 提取通用 `encode-message`、新增 `encode-image-message`；`protocol-codec.clj` encoder 扩展支持 `BufferedImage`（自动转 PNG）；`server.clj` `:image` handler 实现 PNG → BufferedImage → 剪贴板；`client.clj` `content-handler` 支持 `Image`；`core.clj` 重新启用图片同步；新增 protocol / codec / integration 测试共 3 个。文件链路待迁移。
#2026-05-19 完成 Phase 2 P1 第二步：文件链路迁移至新协议。`protocol.clj` 新增 `encode-file-list-message`；`util.clj` 新增 `files->zip-bytes` / `zip-bytes->files`；`protocol-codec.clj` encoder 扩展支持 `List<File>`（自动转 zip）；`server.clj` `:file-list` handler 实现 zip → 临时目录 → 剪贴板；`client.clj` `content-handler` 支持文件列表；`core.clj` 重新启用文件同步；新增 protocol / codec / integration 测试共 3 个。Phase 2 P1 全部完成。
#2026-05-19 完成 Phase 2 P1 第三步：服务端限制最大 payload。`config.clj` 新增 `:max-frame-size` 默认 10MB；`protocol-codec.clj` 的 `->protocol-decoder` 增加可选 `max-frame-size` 参数，超限立即拒绝；`server.clj` `Server` record 增加 `max-frame-size` 字段；`core.clj` 启动时透传配置；新增 config / codec 测试共 2 个。Phase 2 全部完成。
#2026-05-19 完成 HMAC 安全修复：`protocol.clj` `decode-message` 中 HMAC 校验由 `(not= (seq ...) (seq ...))` 改为 `java.security.MessageDigest/isEqual`，消除定时攻击漏洞。`lein test` 48 测试 / 138 断言全绿。
#2026-05-19 完成 app 重复启动保护与 server stop 竞态修复：`app.clj` `start!` 启动前先调用 `stop!` 清理旧实例；`server.clj` `start-server` 用 promise 替代 atom 传递 channel，`stop!` 通过 `(deref promise 10000 nil)` 等待 channel 绑定完成后再关闭。新增 `app-double-start-is-idempotent` 测试。`lein test` 49 测试 / 141 断言全绿。
#2026-05-19 完成 Phase 3 P0 第一步：配置中自动生成并持久化 `node-id`。`config.clj` 新增 `node-id-path`（`~/.lan-clip/node-id`）与 `load-or-create-node-id`，`load-config` 在配置缺少 `:node-id` 时自动调用；用户配置中的 `:node-id` 始终优先。新增 config 测试 3 个；修复 `read-edn-file` nil path NPE。`lein test` 52 测试 / 147 断言全绿。
#2026-05-19 完成 Phase 3 P0 第二~四步：新增 `lan-clip.message-cache` 命名空间（`create-cache` / `put!` / `contains?`，基于 atom + vector 的 LRU 淘汰）；`protocol.clj` 每条消息已自动生成 `message-id`（UUID），header 已携带 `origin-node-id` 与 `sender-node-id`。新增 message-cache 测试 5 个。`lein test` 57 测试 / 162 断言全绿。
#2026-05-19 完成 Phase 3 P0 第五步：远端写入剪贴板后记录 `last-remote-fingerprint`。`server.clj` `handle-msg` 各方法返回 `ClipboardData` 指纹；`->handler` 接受可选 `on-apply` 回调；`start-server` 透传回调。`app.clj` 新增 `last-remote-fp` atom 与 `last-remote-fingerprint` 查询函数，启动时向 server 传入指纹回调，停止后清空。新增 server/app 测试共 5 个。`lein test` 62 测试 / 182 断言全绿。
#2026-05-20 完成 Phase 3 P1 第一步：watcher 识别远端刚写入的内容并抑制回发。`core.clj` `listen-clipboard` 接收 `last-remote-fp` 参数；当剪贴板指纹与 `last-remote-fp` 匹配时判定为回环，抑制发送并输出 `loop-suppressed`。`app.clj` 将 `last-remote-fp` 传递给 `clipboard-handler` 回调。新增 core 测试 2 个，更新 app 测试 handler 签名。`lein test` 64 测试 / 186 断言全绿。
#2026-05-20 完成 Phase 3 P1 第二步：日志区分 `local-change`、`remote-apply`、`loop-suppressed`。`core.clj` `listen-clipboard` 在非回环分支输出 `local-change: text/image/file-list`；`server.clj` `handle-msg` `:text`/`:image`/`:file-list` 方法在写入剪贴板后输出 `remote-apply: <type>`。新增 core 测试 2 个（local-change / loop-suppressed 日志），server 测试 3 个（remote-apply 日志）。`lein test` 69 测试 / 194 断言全绿。
#2026-05-20 完成 Phase 3 P1 第三步：新增 `lan-clip.acceptance` 命名空间（`run-text-sync` / `run-loop-suppression`），提供 localhost 双端自动化验收函数；新增 `lan-clip.acceptance-test` 测试 3 个（文本同步 / 回环抑制 / 双向互通）。`lein test` 72 测试 / 200 断言全绿。Phase 3 全部完成。
#2026-05-20 完成 Phase 4 P0 第一~二步：`config.clj` 新增 `:received-files-dir` 配置项与 `default-received-files-dir` 函数；当用户未指定时，`load-config` 自动回退到 `~/.lan-clip/received-files`。新增 config 测试 3 个（配置项存在 / 默认值 / 自定义覆盖）。`lein test` 75 测试 / 207 断言全绿。
#2026-05-20 完成 Phase 6 P0 状态页：`api.clj` 增加 CORS 头支持；`ui/src-tauri/src/lib.rs` 移除模板 `greet` 命令、新增 `sidecar_port` 命令；前端新增 `src/api.ts`（EDN 解析 / sidecar HTTP 调用 / Tauri invoke 封装）及 5 个 vitest 用例；重写 `App.tsx` 为状态页（sidecar 启停、同步启停、节点名、监听端口、自动刷新）。`lein test` 105 测试 / 305 断言全绿，`cargo test` 2 测试全绿，`npm run build` 通过。
#2026-05-20 完成 Phase 6 P0 配置页：`config.clj` 新增 `:device-name` 配置项（默认空字符串，热更新）；`hot-reloadable-keys` 加入 `:device-name`；前端 `api.ts` 新增 `saveConfig`（PUT /config，camelCase→kebab EDN 序列化）及 2 个 vitest 用例；`App.tsx` 新增 tab 导航（状态/配置）与 `ConfigPage` 表单（设备名、端口、目标主机/端口、共享密钥、轮询间隔、文件大小限制、接收目录）；`App.css` 新增 tabs、config-form、info-banner 样式。`lein test` 107 测试 / 311 断言全绿，`cargo check` 通过，`npm run build` 通过，前端 7 测试全绿。
#2026-05-20 完成 Phase 6 P2 关于页：`App.tsx` 新增 "关于" Tab；使用 Tauri `getVersion` API 获取应用版本；展示协议版本（当前为 1）；添加检查更新占位按钮；补充 vitest 用例 1 个（关于页渲染）。前端 13 测试全绿，`npm run build` 通过。
#2026-05-20 完成 Phase 6 P1 托盘增强：`ui/src-tauri/src/lib.rs` 新增 `received_files_dir()`（返回 `~/.lan-clip/received-files`）与 `open_directory()`（跨平台打开目录）；托盘菜单新增 "打开接收目录" 项；补充 Rust 单元测试 2 个。`cargo test` 6 测试全绿，`cargo check` 通过。
#2026-05-20 完成 Phase 6 P1 托盘同步切换：`App.tsx` 新增 `tray-sync-toggle` 事件监听（使用 `useRef` 避免 stale closure）；Rust 托盘菜单新增 "切换同步" 项（通过 `Emitter` trait 发射事件到前端）；补充 vitest 用例 2 个（运行中停止 / 已停止启动）。前端 15 测试全绿，`cargo test` 6 测试全绿。Phase 6 P1 托盘全部完成。
#2026-05-20 完成 Phase 6 P2 系统通知：新增 `notifications.ts` 封装 `sendNotification` API；`App.tsx` 在 sidecar 启动失败和同步错误时调用 `notifyError`；Rust 注册 `tauri-plugin-notification` 插件；capabilities 增加 `notification:default` 权限；补充 vitest 用例 3 个（notifyError 调用 / sidecar 失败通知）。前端 17 测试全绿，`cargo test` 6 测试全绿。Phase 6 全部完成（sidecar 打包除外）。
#2026-05-21 完成 sidecar 真实进程管理：`ui/src-tauri/src/lib.rs` 中 `SidecarState` 从仅修改 `bool` 的占位实现改为通过 `std::process::Command` 启动 `java -jar` 运行 Clojure uberjar；`sidecar_start` 自动查找 uberjar 路径（开发模式从 `CARGO_MANIFEST_DIR` 推导，生产模式从可执行文件目录查找）；`sidecar_stop` kill 子进程并 `wait`；`sidecar_status` 通过 `child.try_wait()` 判断进程存活；应用 `Exit` 事件确保调用 `stop` 清理。补充 Rust 单元测试 1 个（find_jar_path 回退逻辑）。`lein test` 109 测试 / 316 断言全绿，`cargo test` 6 测试全绿。
#2026-05-21 完成 Tauri 资源打包配置：`tauri.conf.json` 新增 `bundle.resources` 将 `target/lan-clip-1.0-standalone.jar` 映射为资源文件；`lib.rs` 重构 `SidecarState.start()` 为 `start_with_path(path)` 并增加路径存在性校验；`sidecar_start` 命令新增 `app_handle` 参数，通过 `app_handle.path().resolve(..., BaseDirectory::Resource)` 支持打包后从应用资源目录加载 uberjar；`resolve_jar_path` 按优先级查找：资源目录 > CARGO_MANIFEST_DIR > 可执行文件目录。`cargo test` 5 测试全绿，`cargo check` 通过，`lein test` 109 测试全绿。
#2026-05-21 完成 Phase 7 P0 macOS 打包与 release checklist：`npm run tauri build` 成功生成 `lan-clip.app`（~70MB）和 `lan-clip_1.0.0_aarch64.dmg`（~98MB）；验证 `.app/Contents/Resources/lan-clip-1.0-standalone.jar` 已正确嵌入；dmg 可正常挂载并包含 `lan-clip.app` + `Applications` 快捷方式。`create-dmg` 缺失导致 `bundle_dmg.sh` 退出码非零，手动重命名后 dmg 可用。新建 `doc/notes/release-checklist.md`，包含 macOS / Windows 构建步骤与验收项。TODO.md 删除已完成项。
#2026-05-21 完成 Phase 7 P2 签名策略与版本号梳理：验证 `.app` 可正常启动（进程存活3秒以上无崩溃）；`codesign -dv` 检查签名状态为 adhoc（无 TeamIdentifier），首次运行将有 Gatekeeper 提示；`ui/package.json` 版本号由 `0.1.0` 统一为 `1.0.0`（与 Tauri 一致）；`release-checklist.md` 新增"签名策略"和"版本号一致性"章节，记录 Apple Developer 注册、公证、版本号对照表等发布前准备工作。
#2026-05-21 修复 TECHNICAL_DEBT.md #7：`core.clj` 删除重复的 `clip-data-changed?` 函数，引入 `lan-clip.fingerprint` 命名空间，`listen-clipboard` 中统一调用 `fingerprint/changed?` 进行剪贴板内容变化检测；同步删除 `comment` 块中对该函数的引用。`lein test` 109 测试 / 316 断言全绿。TECHNICAL_DEBT.md 删除对应条目。
#2026-05-21 修复 TECHNICAL_DEBT.md #11：删除未使用的遗留文件 `src/lan_clip/socket/content.clj`（`Content` deftype）；同步更新 `project.clj` `:prep-tasks` 移除对该文件的 compile 步骤。`lein test` 109 测试 / 316 断言全绿。TECHNICAL_DEBT.md 删除对应条目。
#2026-05-21 修复 TECHNICAL_DEBT.md #5：`api.clj` 定义 `sensitive-keys` 集合 `#{:secret-key :log-file :received-files-dir}`，`safe-config` 改用 `apply dissoc` 过滤所有敏感字段；同步更新 `api_test.clj` 中 `api-config-returns-default-config-when-not-running` 断言（去除 `:log-file` 应存在的旧断言，增加 `:log-file` 和 `:received-files-dir` 不应存在的断言）；新增 `api-safe-config-filters-sensitive-keys` 单元测试。`lein test` 110 测试 / 321 断言全绿。TECHNICAL_DEBT.md 删除对应条目。
#2026-05-21 修复 TECHNICAL_DEBT.md #5（log.clj）：`log!` 中对 `println` 调用加 `locking *out*`，消除多线程日志行交错问题。`lein test` 110 测试 / 321 断言全绿。TECHNICAL_DEBT.md 删除对应条目。
#2026-05-21 修复 TECHNICAL_DEBT.md #2：`server.clj` `->handler` docstring 中补充单消息短连接设计决策注释，说明每个 TCP 连接只处理一条消息、处理完后立即关闭连接（`(.close ctx)`），以及该设计简化状态管理的权衡。`lein test` 110 测试 / 321 断言全绿。TECHNICAL_DEBT.md 删除对应条目。
#2026-05-21 修复 TECHNICAL_DEBT.md #2（server.clj stop 阻塞）：`start-server` 的 `finally` 块中增加 `(when-not (realized? channel-promise) (deliver channel-promise nil))`，确保启动失败（如端口被占用）时 promise 被立即 deliver 为 `nil`，`stop!` 不再阻塞 10 秒。新增 `server-stop-does-not-block-when-start-fails` 测试验证端口占用场景下 `stop!` 在 1 秒内返回。`lein test` 111 测试 / 322 断言全绿。TECHNICAL_DEBT.md 删除对应条目。
#2026-05-21 修复 TECHNICAL_DEBT.md #10：删除 `core.clj` 中废弃的 `lan-clip` 入口函数及其对 `util/set-interval` 的调用；删除 `util.clj` 中不可停止的 `set-interval` 函数。`lein test` 111 测试 / 322 断言全绿。TECHNICAL_DEBT.md 删除对应条目。
#2026-05-21 修复 TECHNICAL_DEBT.md #11：`watcher.clj`、`client.clj`、`server.clj` 中裸 `.printStackTrace` 统一替换为 `log/log! :error`；三个文件均引入 `lan-clip.log` 命名空间。`lein test` 111 测试 / 322 断言全绿。TECHNICAL_DEBT.md 删除对应条目。
#2026-05-21 修复 TECHNICAL_DEBT.md #13：`api.clj` CORS 由固定 `*` 改为根据请求 Origin 动态回显，只允许 `http://localhost` 和 `tauri://localhost`；无 Origin 头时默认回退到 `http://localhost`。同步更新 `api_test.clj` 中 CORS 相关断言。`lein test` 111 测试 / 322 断言全绿。TECHNICAL_DEBT.md 删除对应条目。
#2026-05-21 修复 TECHNICAL_DEBT.md #15：`protocol.clj` `encode-message` 中在 `ByteBuffer/allocate` 前加入显式上限检查，当 `total-size` 超过 `Integer/MAX_VALUE` 时抛出 `ex-info`（`:cause :message-too-large`），消除隐式 long→int 转换的静默溢出风险。`lein test` 111 测试 / 322 断言全绿。TECHNICAL_DEBT.md 删除对应条目。
#2026-05-21 修复 TECHNICAL_DEBT.md #1：`core.clj` 新增 `in-flight-send` atom 与 `send-client` 辅助函数，采用 latest-wins 策略：启动新发送 future 前先 `future-cancel` 上一个仍在飞行中的 future，消除剪贴板快速变化时堆积未限制 future 的问题。新增 `send-client-cancels-previous-in-flight` 测试。`lein test` 112 测试 / 324 断言全绿。TECHNICAL_DEBT.md 删除对应条目。
#2026-05-21 修复 TECHNICAL_DEBT.md #4：`api.clj` `PUT /config` 增加三层输入验证：请求体大小上限（64KB）、未知 key 拒绝、类型校验（端口范围 / 正整数 / 字符串 / UUID）；`config.clj` `valid-port?` 由 `defn-` 改为 `defn` 供 api 复用。新增 3 个 api 测试（未知 key / 非法端口 / 请求体过大）。`lein test` 115 测试 / 332 断言全绿。TECHNICAL_DEBT.md 删除对应条目。
#2026-05-21 修复 TECHNICAL_DEBT.md #7：前端 `api.ts` 引入 `edn-data` 库替换脆弱的字符串级 EDN 解析器。删除 `parseEdnLike`、`parseEdnVector`、`configToEdn` 等 6 个简易函数；新增 `parseEdnResponse`、`convertKeys`、`isUuidObj` 等辅助函数，支持嵌套 map、vector、tagged literal（#uuid / #inst）的正确解析。`npm run test` 17 测试全绿，`npm run build` 通过，`lein test` 115 测试 / 332 断言全绿。同步删除 TECHNICAL_DEBT.md 中已完成的 P1 #9（Sidecar 真实进程管理已在 2026-05-21 完成）。
#2026-05-21 修复 TECHNICAL_DEBT.md #16：`lib.rs` 中托盘菜单和窗口事件的 `let _ = ...` 错误忽略全部替换为 `if let Err(e) = ... { log::warn!(...) }`。`Cargo.toml` 新增 `log = "0.4"` 依赖。覆盖点：窗口隐藏/显示/焦点、托盘同步事件发射、打开接收目录、应用退出时停止 sidecar。`cargo test` 5 测试全绿，`lein test` 115 测试 / 332 断言全绿，`npm run test` 17 测试全绿。TECHNICAL_DEBT.md 删除对应条目。
#2026-05-21 修复 TECHNICAL_DEBT.md #14（测试 Thread/sleep）：`api_test.clj` 和 `sidecar_test.clj` 中 http-kit 的 start/stop 是完全同步的，删除全部 25 个 `(Thread/sleep 200)`，测试运行时间减少约 5 秒；`watcher_test.clj` 引入 `java.util.concurrent.CountDownLatch` 替代固定 sleep，`watcher-callback-executed` 和 `watcher-is-periodic` 改为在回调触发时立即放行，消除快机器上的等待浪费。`lein test` 115 测试 / 332 断言全绿。TECHNICAL_DEBT.md 中 P2 项全部清零。`app_test.clj`、`server_test.clj`、`acceptance_test.clj` 中涉及 Netty 异步 bind 和消息处理的 sleep 留待后续用 promise 重构。
#2026-05-21 完成 Phase 7 macOS 构建验证：`npm run tauri build` 成功生成 `lan-clip.app`（~70MB）和 `lan-clip_1.0.0_aarch64.dmg`（~61MB）；验证 `.app/Contents/Resources/lan-clip-1.0-standalone.jar` 已正确嵌入。`lein test` 115 测试 / 332 断言全绿，`cargo test` 5 测试全绿，`npm run test` 17 测试全绿。Phase 7 剩余 P0/P1 项（文本/图片/文件互传、防火墙提示、残留进程等）为手动验收，需人工介入。
#2026-05-21 配置 GitHub Actions CI 自动构建：新增 `.github/workflows/build.yml`，矩阵构建 macOS（`.app` + `.dmg`）和 Windows（`.msi` + `.exe`）。流程：checkout → Java/Leiningen → `lein uberjar` → Node.js/Rust → `npm run tauri build` → 上传 artifact。修复两个问题：`package.json` 补全缺失的 `@tauri-apps/plugin-notification` 依赖；Windows runner 上用 `java -jar $LEIN_JAR -m leiningen.core.main uberjar` 启动 Leiningen（PowerShell 引号解析问题）。CI 构建全绿，产物可下载。
#2026-05-21 完成 Phase 8 P1 开机自启动：`ui/src-tauri/Cargo.toml` 添加 `tauri-plugin-autostart`；`lib.rs` 注册插件（macOS 使用 `LaunchAgent`）；`capabilities/default.json` 增加 `autostart:default` 权限；前端 `api.ts` 封装 `enableAutostart` / `disableAutostart` / `getAutostartStatus`；`App.tsx` 配置页新增"开机自启动"复选框；`App.css` 增加 `.checkbox-label` 样式。`cargo test` 5 测试全绿，`npm run test` 17 测试全绿，`npm run build` 通过。TODO.md 删除对应项。
#2026-05-21 完成 Phase 8 P1 自动更新能力：`ui/src-tauri/Cargo.toml` 添加 `tauri-plugin-updater`；`lib.rs` 以 `tauri_plugin_updater::Builder::new().build()` 注册插件；`tauri.conf.json` 配置 updater（endpoint 指向 GitHub releases `latest.json`、Ed25519 公钥）；`capabilities/default.json` 增加 `updater:default` 权限；前端 `api.ts` 封装 `checkForUpdate`；`App.tsx` 关于页"检查更新"按钮接入真实逻辑（检测中/已最新/发现新版/出错四种状态）。`lein test` 115 测试 / 332 断言全绿，`cargo test` 5 测试全绿，`npm run test` 17 测试全绿。TODO.md 删除对应项。
#2026-05-21 完成 Phase 8 P2 发送失败重试：`config.clj` `default-config` 新增 `:retry-count 3` 和 `:retry-delay-ms 1000`；`client.clj` 新增 `run-with-retry` 函数，连接/发送失败时按配置自动重试，成功立即返回，最终失败抛出异常并记录错误日志；`core.clj` `send-client` 从 `app/current-config` 读取重试参数后调用 `run-with-retry`；正确处理 `InterruptedException`（被 `future-cancel` 时不进入重试，直接抛出）。新增 `lan-clip.socket.client-test` 4 个单元测试（首次成功/重试后成功/最终失败/默认值）。`lein test` 119 测试 / 338 断言全绿，`cargo test` 5 测试全绿，`npm run test` 17 测试全绿。TODO.md 删除对应项。
#2026-05-21 完成 Phase 8 P2 增加传输历史：新增 `lan-clip.history` 命名空间（`create-store` / `record!` / `recent` / `clear!`，原子+向量环形缓冲区，默认最多 100 条，仅记录元数据不存内容）；`app.clj` 集成 `history-store`，`start!` 创建、`stop!` 清空；`core.clj` 发送端在三种 flavor 处理中调用 `record-send-history!`；`server.clj` 接收端在处理消息后记录接收历史；`api.clj` 新增 `GET /history/recent` 端点。前端 `api.ts` 新增 `fetchHistory` 与 `HistoryEntry` 类型；`App.tsx` 新增"历史"Tab 与 `HistoryPage` 组件（方向/类型格式化、彩色标签、时间/大小/对端展示）。新增 `history_test.clj` 7 个测试（创建默认值/记录与查询/limit 参数/最大条数淘汰/清空/空存储/默认 limit）。`lein test` 126 测试 / 349 断言全绿，`cargo test` 5 测试全绿，`npm run test` 17 测试全绿。TODO.md 删除对应项。
#2026-05-21 完成 Phase 8 P2 增加设备发现：新增 `lan-clip.discovery` 命名空间（`create-registry` / `register-peer!` / `recent-peers` / `start-discovery`）；采用 UDP 广播 beacon 机制（端口 9616，周期 3 秒，TTL 30 秒），beacon 携带 node-id、device-name、port、protocol-version；`app.clj` 集成 discovery 生命周期（`start!` 启动、`stop!` 停止）；`api.clj` 新增 `GET /peers` 端点。前端 `api.ts` 新增 `fetchPeers` 与 `Peer` 类型；`App.tsx` 配置页新增"已发现设备"区块（自动刷新、点击填充目标主机/端口）。新增 `discovery_test.clj` 7 个测试（空注册表/注册/过期过滤/包含新鲜/排除本节点/按名排序/覆盖更新）。`lein test` 133 测试 / 362 断言全绿，`cargo test` 5 测试全绿，`npm run test` 17 测试全绿。TODO.md 删除对应项。
#2026-05-21 完成 Phase 8 P2 增加配对流程：扩展 `lan-clip.discovery` 支持定向 UDP 配对消息（`:pair-request` / `:pair-response`），与 beacon 共用端口 9616；`app.clj` 新增 `handle-pair-request`（自动接受并更新配置：target-host/target-port/secret-key）和 `handle-pair-response`（接受时更新 target-host/target-port）；新增 `initiate-pairing!` 函数，生成随机 UUID 作为共享密钥并通过 discovery socket 发送 pair-request；`api.clj` 新增 `POST /pair` 端点。前端 `api.ts` 新增 `initiatePairing`；`App.tsx` 已发现设备列表中每个 peer 新增"配对"按钮，配对成功后自动填充目标配置和共享密钥。新增 `pairing_test.clj` 6 个测试（消息路由/请求处理/响应处理/接受更新配置/拒绝不更新/配置持久化验证）。`lein test` 139 测试 / 374 断言全绿，`cargo test` 5 测试全绿，`npm run test` 17 测试全绿。TODO.md 删除对应项。
#2026-05-21 完成 Phase 8 P2 增加多 peer 同步策略：`core.clj` 改为向所有发现的 peer 广播发送（`active-peers` + `send-to-all-peers!`），每个 peer 独立维护 in-flight future（latest-wins 取消策略，`send-to-peer` + `in-flight-sends` map atom）；`api.clj` `/status` 返回 `:peer-count`；前端状态页显示活跃 peers 数量。新增 `api_test.clj` 1 个测试（peer-count）。`lein test` 140 测试 / 378 断言全绿，`cargo test` 5 测试全绿，`npm run test` 17 测试全绿。TODO.md 删除对应项。
